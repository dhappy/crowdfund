%table
  %thead
    %tr
      %th{ colspan: 3 }= @project.name
    %tr
      %th Name
      %th Bids
      %th Bounties
  %tbody
    - @project.issues.each do |issue|
      %tr
        %td= link_to issue.name, issue.url
        %td= link_to (issue.leading_bid ? number_to_currency(issue.leading_bid.try(:amount)) : '✖'), new_bid_path(issue: issue.id)
        %td= link_to issue.total_bounties, new_bounty_path(issue: issue.id)
