%table
  %thead
    %tr
      %th{ colspan: 3 }= @project.name
      %th
        %th Name
        %th Bids
        %th Bounties
  %tbody
    - @project.issues.each do |issue|
      %tr
        %td= issue.name
        %td= issue.leading_bid
        %td= issue.total_bounties
