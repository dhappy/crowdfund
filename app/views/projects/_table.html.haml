%table
  %thead
    %tr
      %th{ colspan: 3 }= @project.name
    %tr
      %th Name
      %th Bids
      %th Bounties
  %tbody
    - @project.issues.each do |issue|
      %tr
        %td= issue.name
        %td= issue.leading_bid
        %td= link_to issue.total_bounties, new_bid_path
